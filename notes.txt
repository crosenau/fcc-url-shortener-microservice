Database interactions
  - Separate js file
  
    -- Search db for input URL.
      * If entry already exists, return db object with original_url and short_url.
    -- Else
      * Do a sorted search by short_url to get max short_url.
      * Create new db entry for input url and max + 1 for it's short_url.
      * Then search for input again and return the newly created db id.
      ** Return this all as a promise which can be .then'd in server.js